#!/bin/bash -e
# This script is meant to be run inside a DietPi image.
# It installs the necessary packages on top of DietPi to create the OS image for Pi-DJ.

cat /boot/dietpi.txt

# TODO: customize the image with the DietPi tools
# TODO: create / customize the dietpi.txt file so that it boots nicely and it easily configurable.
